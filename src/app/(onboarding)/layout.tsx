/**
 * Onboarding layout: minimal, focused, no chrome beyond the logo — the
 * student is mid-setup (PHASE_1_UX_PLAN.md §3).
 */
export default function OnboardingLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
